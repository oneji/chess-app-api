# Chess application API
Chess application backend written on Node.js

### Installation
To run the API on your local machine do the following:
```sh
$ npm install
$ nodemon server.js
```

It will install all dependencies and run the project.
